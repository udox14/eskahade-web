// Root layout — minimal wrapper. Nested route groups ((site) and admin)
// each declare their own html/body with fonts and styles.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
